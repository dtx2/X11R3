/* 
 * crtstub : stubs for displays not linked into the server
 */

#include "crtstub.h"

ScreenInit(topcatScreenInit,"Topcat")
ScreenInfo(topcatScreenInfo,"Topcat")
ScreenClose(topcatScreenClose,"Topcat")

	/* el sleaso stubs */
tcWholeGlyph() {  }
