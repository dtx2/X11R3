LOCAL_LIBRARIES = $(XLIB)
        DEFINES = -DNOINLINE

           SRCS = xpr.c x2pmp.c
           OBJS = xpr.o x2pmp.o

ComplexProgramTarget(xpr)
InstallScript(xdpr,$(BINDIR))
InstallManPage(xdpr,$(MANDIR))
